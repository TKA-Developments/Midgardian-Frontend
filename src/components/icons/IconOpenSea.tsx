import { Icon } from "./Icon";

export const IconOpenSea = (props: Icon) => {
  return (
    <img
      src="https://storage.googleapis.com/opensea-static/Logomark/Logomark-Blue.svg"
      width={40}
      height={40}
    />
  );
};
